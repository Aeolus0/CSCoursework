---
- hosts: All
  remote_user: root
  tasks:
    # Begin default apps
    apt: name={{items}} state=present
    with_items:
      - htop
      - tmux
      - screen
      - pastebinit
    # End generalized